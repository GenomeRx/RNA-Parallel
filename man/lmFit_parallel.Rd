% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/limma_lmFit_parallel.R
\name{lmFit_parallel}
\alias{lmFit_parallel}
\title{limma's lmFit with its row loops parallelised}
\usage{
lmFit_parallel(
  object,
  design = NULL,
  ndups = NULL,
  spacing = NULL,
  block = NULL,
  correlation,
  weights = NULL,
  method = "ls",
  ...,
  workers = NULL,
  chunks = NULL,
  parallel_backend = getOption("combat.backend", "mclapply"),
  backend = NULL,
  label = NULL
)
}
\arguments{
\item{object}{Anything \code{limma::lmFit} accepts: matrix, \code{EList} from
\code{\link[limma:voom]{limma::voom()}}, \code{MAList}, \code{ExpressionSet}, \code{PLMset}, \code{marrayNorm}, numeric data frame.
\code{EListRaw} and \code{RGList} are refused by limma itself with \"please normalize first\".}

\item{design}{Design matrix, samples in rows.}

\item{ndups}{Rows containing each gene. Only 1 is supported, see refusals above.}

\item{spacing}{Spacing between duplicate rows.}

\item{block}{Blocking vector, one entry per array. Non-\code{NULL} routes to \code{gls.series}.}

\item{correlation}{Inter-duplicate or inter-block correlation. No default, exactly as in
\code{lmFit}; leave it missing and the blocked branch stops the way \code{lmFit} does.}

\item{weights}{Numeric matrix of per-observation weights, a per-gene vector, or a
per-array vector.}

\item{method}{\code{"ls"}. \code{"robust"} is refused, see refusals above.}

\item{...}{Passed to \code{gls.series}, and from there to \code{duplicateCorrelation}.}

\item{workers}{Maximum concurrent worker processes. \code{NULL}, the default, resolves to
\code{min(8, detectCores() - 2)}: 8 is where the measured curves flatten, and the two
held back leave the master and the rest of the machine a core each. Pass a smaller
number when a second R process is also forking; six workers alongside one
kernel-panicked a 24 GB machine, and no core count read here can see that session.

Going past your PERFORMANCE core count can be slower than stopping at it, and the loss is
worst on the largest inputs -- which is exactly where people reach for more workers.
Measured on an 8-core machine with 4 performance cores, TMM on 15,000 genes by 9,000
specimens: 4 workers 10.74 s, 6 workers 7.26 s, 8 workers 11.60 s. Eight was slower than
four. The default resolved to 6 there and was optimal; raising it by hand made it worse.}

\item{chunks}{Row chunks. Defaults to \code{workers}; passing \code{chunks = workers} explicitly is redundant.
Clamped so no chunk holds one gene.}

\item{parallel_backend}{One of \code{\link[=combat_backends]{combat_backends()}}, or a function
\verb{function(idx, f, workers)} returning a list in the order of \code{idx}. Defaults to
\code{getOption("combat.backend", "mclapply")}.}

\item{backend}{Optional \code{lmFit} to wrap. Defaults to \code{limma::lmFit}.}

\item{label}{Optional name for this call in the timing line, when
\code{options(combat.timing = TRUE)} is set. Defaults to the companion and the matrix shape,
e.g. \verb{ComBat-seq 18,270 x 1,500}; pass a cohort name to tell calls apart in a loop.}
}
\value{
An \code{MArrayLM}, \code{identical()} to what the backend's own \code{lmFit} returns for the
same input. Under either size gate, on a branch a block would flip, or at
\code{workers = 1}, that is literally what it is: one plain call to the backend with a
dispatch wrapper around it and no parallelism added.
}
\description{
Runs \code{\link[limma:lmFit]{limma::lmFit()}} itself. The algorithm is not reimplemented and not copied: limma's
own \code{lmFit} is called with \code{lm.series} and \code{gls.series} rebound in a child of its own
environment, so those two resolve to row-parallel versions while \code{getEAWP},
\code{nonEstimable}, \code{asMatrixWeights}, \code{duplicateCorrelation} and \code{new("MArrayLM", ...)}
still resolve to limma's. Output is \code{identical()} to what \code{limma::lmFit} returns for the
same input, on the whole \code{MArrayLM}, not on selected slots.
}
\details{
\code{lm.series} and \code{gls.series} are where a voom pipeline actually spends its time:
19.25\% of a plain one and 12.80\% of a blocked one.
}
\section{What a row block reinterprets, and how that is handled}{

A limma row fitter reads three things off the shape of the matrix it is handed, so a
block can quietly answer a different question than the whole matrix would. All three
were measured against limma 3.62.2.

\code{asMatrixWeights} tests its gene branch (\code{lw == 1 || lw == dim[1]}) before its array
branch (\code{lw == dim[2]}), so a bare length-narrays weight vector is read as GENE weights
by any block whose row count happens to equal the array count. Splitting a 40 by 8
matrix into 8-row blocks moved coefficients by 0.325. Weights are therefore expanded to a
full matrix ONCE, against the full dimensions, and row-subset with limma's own
\code{arrayweights} attribute carried along.

\code{NoProbeWts} selects between two numerically different algorithms that also return
different component sets: the fast path returns \code{lm.fit}'s whole object including \code{qr}
and \code{assign}, the slow path a bare seven-element list without them. One NA cell in a 400
gene matrix put the whole matrix on the slow path and every block on the fast one, and
114 of 400 sigma differed. Splitting is refused, in favour of one plain vendor call,
unless every block provably lands on the same side as the full matrix.

\code{stats::lm.fit} demotes a one-column response with \code{if (is.matrix(y) && ny == 1L) y <- drop(y)}, and limma feeds it \code{t(M)}, so a one-gene block switches sigma from
\code{colMeans} to \code{mean} and loses the gene name: 4,080 of 16,000 one-gene blocks returned a
different sigma and all 2000 lost the name. Every split here holds at least two genes.
}

\section{Consensus correlation}{

\code{correlation} has no default in \code{lmFit} and the blocked branch tests
\code{missing(correlation)}, so missingness is forwarded rather than turned into \code{NULL}. An
explicit \code{correlation = NULL} does reach \code{gls.series}, which then calls
\code{duplicateCorrelation} itself, a trimmed mean over whatever genes it was given. Per
block that is a different consensus for every block. It is resolved once here, on the
full matrix, and the resulting scalar is what the blocks see.
}

\section{Dispatches too small to be worth a fork}{

The two branches of \code{lm.series} are not the same job. With probe weights, or an \code{EList}
from \code{\link[limma:voom]{limma::voom()}}, limma runs an R loop over genes, and that forks well: measured
2.52x unblocked and 2.85x blocked on 20,000 genes by 24 samples, 2.96x and 3.39x on
60,000 by 48, against 0.95x at 20,000 cells. \code{getOption("combat.min.cells", 20000)}, the
same gate ComBat-seq uses, lands on that break-even and is what this branch takes.

Without probe weights limma fits every gene in one vectorised \code{lm.fit}, which costs
milliseconds and is mostly not worth handing to another process. That branch measured
0.24x at 1.2 million cells and 0.56x at 4.8 million, turning over to 1.47x at 12 million
and 1.71x at 20 million. It gets its own, much higher
\code{getOption("combat.min.ls.cells", 6e6)}; sharing one gate made it four times slower than
calling limma directly. Cells is only a proxy, since the cost rises with array count
too, so the gate sits at the smallest size measured to win. Set either option to 0 to
dispatch unconditionally; output is identical either way.
}

\section{Configurations this refuses}{

\code{method = "robust"} goes to \code{mrlm}, and \code{ndups >= 2} makes \code{unwrapdups} reshape rows so
that a gene no longer occupies one row. Neither is split, and both stop with an error
rather than returning a serial result that looks parallel.
}

\examples{
\donttest{
if (requireNamespace("limma", quietly = TRUE)) {
  set.seed(1)
  y <- matrix(rnorm(4000), nrow = 500)
  design <- cbind(Intercept = 1, Group = rep(0:1, each = 4))

  fit <- lmFit_parallel(y, design, workers = 4L)
  identical(fit, limma::lmFit(y, design))
}
}
}
