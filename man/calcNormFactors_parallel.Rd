% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/edger_norm_parallel.R
\name{calcNormFactors_parallel}
\alias{calcNormFactors_parallel}
\title{edgeR normalisation factors with the per-column work parallelised}
\usage{
calcNormFactors_parallel(
  object,
  lib.size = NULL,
  method = c("TMM", "TMMwsp", "RLE", "upperquartile", "none"),
  refColumn = NULL,
  logratioTrim = 0.3,
  sumTrim = 0.05,
  doWeighting = TRUE,
  Acutoff = -1e+10,
  p = 0.75,
  ...,
  workers = 4L,
  chunks = NULL,
  parallel_backend = getOption("combat.backend", "mclapply"),
  backend = NULL
)
}
\arguments{
\item{object}{Count matrix, genes in rows and samples in columns, or a \code{DGEList}.}

\item{lib.size}{Library sizes. Defaults to \code{colSums(object)}, computed by edgeR on the
whole matrix.}

\item{method}{Normalisation method, one of \code{"TMM"}, \code{"TMMwsp"}, \code{"RLE"},
\code{"upperquartile"} or \code{"none"}.}

\item{refColumn}{Reference column for TMM and TMMwsp. Chosen by edgeR when \code{NULL}.}

\item{logratioTrim, sumTrim, doWeighting, Acutoff}{TMM trimming and weighting, passed
through unchanged.}

\item{p}{Quantile for \code{method = "upperquartile"}.}

\item{...}{Ignored, as in edgeR.}

\item{workers}{Maximum concurrent worker processes. 4 is the default because 6 has
kernel-panicked a 24 GB machine when a second R process was also forking.}

\item{chunks}{Column chunks per dispatch. Defaults to \code{workers}.}

\item{parallel_backend}{One of \code{\link[=combat_backends]{combat_backends()}}, or a function
\verb{function(idx, f, workers)} returning a list in the order of \code{idx}. Defaults to
\code{getOption("combat.backend", "mclapply")}.}

\item{backend}{Optional \code{calcNormFactors} default method to wrap. Defaults to edgeR's.}
}
\value{
For a matrix, the named numeric vector \code{edgeR::calcNormFactors} returns. For a
\code{DGEList}, the object with \verb{$samples$norm.factors} replaced, exactly as
\code{edgeR::calcNormFactors} returns it.
}
\description{
Runs edgeR's own \code{calcNormFactors}. The method is not reimplemented and not copied:
\code{calcNormFactors.default} is called once, unchanged, on the whole matrix, with
\code{.calcFactorTMM}, \code{.calcFactorTMMwsp}, \code{.calcFactorRLE} and \code{.calcFactorQuantile} rebound
in a child of edgeR's own namespace. Every other symbol in the body still resolves to
edgeR's own code, so \code{colSums}, the all-zero row filter, the reference column choice, the
RLE geometric mean and the final centering are edgeR's, computed once, on every column.
Output is \code{identical()} to \code{edgeR::calcNormFactors} for the same input, not merely close.
}
\section{The two wins}{

\code{.calcFactorTMM} builds its trim mask from \code{rank(logR)} and \code{rank(absE)} twice each,
which is four sorts where two would do. \code{rank} is rebound to compute each vector once,
which is bit-identical and needs no workers at all: measured 1.28s to 0.81s on
20000 x 200, \code{identical()} TRUE, max abs diff 0. On top of that the vendor's per-column
loops are dispatched across workers, taking the same call to 0.29s at four workers.
}

\section{Why the axis is columns}{

Every method here ranks, medians or takes a quantile across genes, so a gene-row block is
not a smaller version of the problem. The per-sample factor is independent only once the
pooled quantities are fixed, which is why the vendor is called once on the full matrix
rather than once per block. Splitting the matrix would also have to rebuild
\code{lib.size <- colSums(x)}, and \code{colSums} over fractional doubles is not block-associative
on arm64, where \code{sizeof.longdouble} is 8: about 1.8e-14 relative, on the quantity every
method divides by.
}

\section{Dispatches too small to be worth a fork}{

A fork costs about 60 ms here, so small matrices are slower parallelised than not, and
each path has its own threshold because they do not cost the same per cell. The TMM and
TMMwsp loops dispatch at or above \code{getOption("combat.min.norm.cells", 2e5)} cells:
measured 0.50x at 1e5, 1.00x at 2e5, 1.85x at 5e5. The RLE and upperquartile loops take
one order statistic per column and cost roughly a fifteenth as much, so they wait for
\code{getOption("combat.min.order.cells", 4e6)}: measured 0.93x at 4e6 and 1.24x at 1e7. The
\code{rank} hoist is not gated, because it never costs anything, and neither is \code{workers = 1}.
}

\examples{
\donttest{
set.seed(1)
counts <- matrix(rnbinom(20000, mu = 50, size = 5), nrow = 1000)
identical(calcNormFactors_parallel(counts, workers = 4L),
          edgeR::calcNormFactors(counts))
}
}
