% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zzz.R
\name{rnaparallel_stale}
\alias{rnaparallel_stale}
\title{Has this package been reinstalled underneath the running session?}
\usage{
rnaparallel_stale()
}
\value{
\code{TRUE} when the installed build differs from the one this session loaded, \code{FALSE}
when they agree, and \code{NA} when the installed build cannot be read at all.
}
\description{
R lazy-loads function bodies by byte offset into \code{rnaparallel.rdb}. Reinstalling the
package rewrites that file, so a session that was already running gets
\verb{lazy-load database ... is corrupt} the first time it touches a symbol it had not yet
forced. Inside a forked worker that surfaces as a failed dispatch, and a caller with a
fallback path can quietly proceed on uncorrected data: three notebook runs were lost to
exactly this before anyone realised the package, not the data, was the problem.
}
\details{
Nothing here prevents it. The fix is to restart R after installing. This only lets a long
run notice, by comparing the build stamp this session loaded against the one on disk now.

Call it defensively. If the reinstall crossed the release that introduced this function, the
loaded namespace does not contain it and the call raises \verb{could not find function} instead of
returning \code{TRUE} -- the checker defeated by the very condition it detects. That error IS the
stale answer, so treat it as one:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{if (isTRUE(tryCatch(rnaparallel_stale(), error = function(e) TRUE))) \{
  stop("rnaparallel was reinstalled under this session; restart R.")
\}
}\if{html}{\out{</div>}}
}
\examples{
if (isTRUE(tryCatch(rnaparallel_stale(), error = function(e) TRUE))) {
  message("rnaparallel was reinstalled under this session; restart R.")
}
}
