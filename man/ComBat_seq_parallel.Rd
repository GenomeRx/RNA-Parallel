% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ComBat_seq_parallel.R
\name{ComBat_seq_parallel}
\alias{ComBat_seq_parallel}
\title{ComBat-seq with its hot paths parallelised}
\usage{
ComBat_seq_parallel(
  counts,
  batch,
  group = NULL,
  covar_mod = NULL,
  full_mod = TRUE,
  shrink = FALSE,
  shrink.disp = FALSE,
  gene.subset.n = NULL,
  workers = 4L,
  chunks = NULL,
  parallel_backend = getOption("combat.backend", "mclapply"),
  backend = NULL
)
}
\arguments{
\item{counts}{Raw count matrix, genes in rows, samples in columns.}

\item{batch}{Batch vector, one entry per column of \code{counts}. Call
\code{droplevels()} on a factor first: ComBat-seq counts samples per level, so a
level left over from subsetting reads as a batch with no samples and it stops
with a message that does not mention levels.}

\item{group}{Optional biological condition, preserved by the correction.}

\item{covar_mod}{Optional model matrix of other covariates to preserve, e.g.
\code{cbind(cov1, cov2)}.}

\item{full_mod}{Fit the full model including \code{group}.}

\item{shrink}{Shrink dispersion estimates towards a common value.}

\item{shrink.disp}{Shrink the dispersion as well as the mean.}

\item{gene.subset.n}{Genes sampled for shrinkage. Seed before comparing two
runs with this set: the original ComBat-seq's \code{monte_carlo_int_NB} calls \code{sample()}, so
unseeded back-to-back runs draw different subsets and differ legitimately.}

\item{workers}{Maximum concurrent worker processes. 4 is the default because 6
has kernel-panicked a 24 GB machine when a second R process was also forking.}

\item{chunks}{Row chunks per parallel step. Defaults to \code{workers}. More chunks
means smaller peak memory per worker and slightly more fork overhead.}

\item{parallel_backend}{One of \code{\link[=combat_backends]{combat_backends()}}, or a function
\verb{function(idx, f, workers)} returning a list in the order of \code{idx}. The
function form plugs in any framework this package does not name, including
\code{parallel::parLapply} over a FORK or PSOCK cluster, \code{pbapply::pblapply},
\code{furrr::future_map}, \verb{doFuture::\%dofuture\%}, or any \code{BiocParallel} BPPARAM.
Defaults to \code{getOption("combat.backend", "mclapply")}.}

\item{backend}{Optional ComBat-seq function to wrap. Defaults to
\code{sva::ComBat_seq}, falling back to a visible top-level \code{ComBat_seq}.}
}
\value{
A gene-by-sample matrix of adjusted counts, \code{identical()} to what the
backend's own \code{ComBat_seq} returns for the same input. \code{workers = 1} takes a
plain \code{lapply} and adds no parallelism, so it is the backend function itself
with a thin dispatch wrapper.
}
\description{
Runs ComBat-seq itself. The algorithm is not reimplemented and not copied: the
original ComBat-seq function is called with six symbols rebound in a child of its own
environment, so \code{glmFit}, \code{glmFit.default}, \code{match_quantiles} and
\code{estimateGLMTagwiseDisp} resolve to row-parallel versions, \code{sapply} dispatches the
per-batch common-dispersion estimate across batches, and \code{lapply} dispatches the
per-batch tagwise estimate the same way, while every other symbol in the
body, \code{vec2mat}, \code{estimateGLMCommonDisp}, \code{getOffset}, \code{monte_carlo_int_NB}, still
resolves to the backend's own. Output is
\code{identical()} to what \code{sva::ComBat_seq} returns for the same input, not merely
close to it.
}
\section{Why a rebind and not a rewrite}{

An earlier version transcribed the algorithm by hand so the loop could be
parallelised, and drifted from the original ComBat-seq twice in ways synthetic test data
could not expose. The gene filter was written \code{!all(x <= 1) && var(x) > 0}
against the original ComBat-seq's per-batch \code{all(x == 0)}, which returned 258 genes
uncorrected and shifted \code{lib.size} for every surviving gene as well. And the
copy dropped the model intercept the original ComBat-seq keeps, which corrupted every
dispersion estimate because \code{mod} also feeds \code{estimateGLMCommonDisp}. Both
passed on uniform Poisson counts and failed only on real sparse data. Rebinding
cannot drift, because there is no second copy of the algorithm to drift.
}

\section{Backends for ComBat-seq itself}{

Works against Bioconductor \code{sva} or a sourced copy of upstream
\url{https://github.com/zhangyuqing/ComBat-seq}, whichever is loaded. Both
expose the same 8-argument signature and the same helpers; the helper is taken
from the backend's own environment, so the two can never be mixed.
}

\section{Backends for the parallelism}{

\code{parallel_backend} selects the framework: \code{"mclapply"} (default, forks),
\code{"future"}, \code{"BiocParallel"}, \code{"foreach"}, or \code{"serial"}. All of them return
bit-identical results, because each row chunk is a pure function of its own
genes and every backend preserves chunk order. See \code{\link[=combat_backends]{combat_backends()}}.

With \code{"future"} the caller owns the plan; this package will not set one, since
that would stamp on a plan established for the whole session. Set e.g.
\code{future::plan(future::multicore, workers = 4)} first.

\code{options(combat.fork = FALSE)} forces a serial run on any backend, with
identical output. That is the escape hatch if forking upsets an IDE. Windows
cannot fork and falls back to serial automatically, so results are correct there
and not faster.
}

\section{Dispatches too small to be worth a fork}{

ComBat-seq calls the quantile match and the dispersion estimate once per batch, not
once per run, so batch count sets how thin each dispatch is. A 100-batch design hands
over a hundredth of the samples at a time, and on a small matrix that is not worth it:
measured 0.50x at 5,000 cells, 0.92x at 10,000 and 1.39x at 20,000.
Dispatches below \code{getOption("combat.min.cells", 20000)} therefore run serially.
Without that gate 500 genes by 1,000 samples across 100 batches ran at 0.80x,
slower than not parallelising at all. Set the option to 0 to restore the old
unconditional behaviour; output is identical either way.

The threshold is per path, because the paths do not cost the same per cell.
\code{qnbinom} in the quantile match earns a fork at 20,000 cells; dispersion
estimation is cheaper and does not break even below 30,000, measuring
0.65x at 10,000 and 0.79x at 20,000, so it has its own
\code{getOption("combat.min.disp.cells", 30000)}, and the GLM fit its own
\code{getOption("combat.min.glm.cells", 1e5)}. One shared threshold made the
dispersion split a net loss on small matrices with many batches.
}

\examples{
\donttest{
if (requireNamespace("sva", quietly = TRUE)) {
  set.seed(1)
  count_matrix <- matrix(rnbinom(1600, mu = 50, size = 5), nrow = 200)
  batch <- rep(1:2, each = 4)

  # upstream's covariate example, with parallelism added
  cov1 <- rep(c(0, 1), 4)
  cov2 <- c(0, 0, 1, 1, 0, 0, 1, 1)
  covar_mat <- cbind(cov1, cov2)
  adjusted_counts <- ComBat_seq_parallel(count_matrix, batch = batch, group = NULL,
                                         covar_mod = covar_mat, workers = 4L)

  identical(adjusted_counts,
            sva::ComBat_seq(count_matrix, batch = batch, group = NULL,
                            covar_mod = covar_mat))
}
}
}
